# Core (solid, work on Python 3.14)
pandas>=2.2
numpy>=1.26
python-dotenv>=1.0
pytest>=8.0

# --- Real Quotex connector ---
# The unofficial pyquotex client is VENDORED in vendor/pyquotex (its PyPI/git
# packaging is broken and pins <4.0 python; vendoring isolates the fragile
# dependency and pins the exact code we tested on Python 3.14.4).
# These are its runtime deps:
certifi>=2025.1.31
httpx>=0.27.0
websockets>=12.0
pyfiglet>=1.0.2
beautifulsoup4>=4.12.3
fake-useragent>=2.2.0
rich>=13.7.0
